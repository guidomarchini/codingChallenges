package declaration.chg5

interface FirstPositive {
    /**
     * Given a list of numbers, returns the first number > 0 not contained in the list.
     */
    fun firstGreaterThanZero(numbers: List<Int>): Int
}